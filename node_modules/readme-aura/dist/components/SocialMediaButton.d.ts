type H = (type: unknown, props: Record<string, unknown> | null, ...children: unknown[]) => unknown;
interface GradientStop {
    offset: string;
    color: string;
}
interface SocialMediaButtonProps {
    icon: string;
    text: string;
    backgroundColor?: string;
    textColor?: string;
    fontSize?: number;
    width?: number;
    height?: number;
    gradientStops?: GradientStop[];
    gradientStrokeWidth?: number;
    borderColor?: string;
    iconSize?: number;
}
/**
 * Pill-shaped social media button with an animated holographic gradient border.
 * Usage in aura blocks:
 *   <SocialMediaButton icon="icons/x-icon.png" text="@handle" />
 * Recommended aura dimensions: width=150 height=40 (match the width/height props)
 */
export declare function makeSocialMediaButton(h: H): ({ icon, text, backgroundColor, textColor, fontSize, width, height, gradientStops, gradientStrokeWidth, borderColor, iconSize, }: SocialMediaButtonProps) => unknown;
export {};
